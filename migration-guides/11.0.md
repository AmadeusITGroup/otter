# Major 11.0 Migration guide

> [!IMPORTANT]
> Please make sure to first follow the ["Upgrade new version guide"](https://github.com/AmadeusITGroup/otter/blob/main/docs/core/UPGRADE_NEW_VERSION.md) before going through these steps.

## @ama-sdk/core

- `prepareOptions` method has been removed from `ApiClient`. `getRequestOptions` should be used instead.
- To support `JSON` specification format and to remove the reference to the outdated `Swagger` format name, the file containing the current specification used to generate the SDK will be stored in a file `open-api.yaml` or `open-api.json` instead of `swagger-api.yaml`.
- To avoid interface name conflicts in case of an API with multiple tags (See issue [#855](https://github.com/AmadeusITGroup/otter/issues/855)), the API parameter object interface named with the pattern `<api-function-name>RequestData` will now be named with the pattern `<api-function-name><api-name>RequestData`.
  - The same renaming is applied to `enum` types in request parameters. It changes from `<api-function-name><parameter-name>Enum` to `<api-name><api-function-name><parameter-name>Enum`.

## @o3r/schematics

- `NodePackageNgAddTask` has been removed. `setupDependencies` should be used instead

- The [`stringifyDate` CLI option](https://www.npmjs.com/package/@ama-sdk/schematics#dates) is now enabled by default. If 
you want to use the `Date` format, you will need to set it to `false` via the command line:

```shell
# with yarn package manager
yarn run schematics @ama-sdk/schematics:typescript-core --global-property stringifyDate=false
# with npm package manager
npm exec schematics @ama-sdk/schematics:typescript-core -- --global-property stringifyDate=false
```

or by disabling it for your generator in your `openapitools.json` file:
```json5
{
  "$schema": "https://raw.githubusercontent.com/OpenAPITools/openapi-generator-cli/master/apps/generator-cli/src/config.schema.json",
  "generator-cli": {
    "version": "7.4.0", // Version of the Codegen downloaded by the cli - updated via the 
    "storageDir": ".openapi-generator",
    "generators": {
      "my-generator": {
        "generatorName": "typescriptFetch",
        "output": ".",
        "inputSpec": "spec-path.(yaml|json)",
        "globalProperty": {
          "stringifyDate": false
        }
      }
    }
  }
}
```

- Prior to version 11, the default types for date and date-time objects were respectively `utils.Date` and `utils.DateTime`.
Those two types take the date from the API response and remove their timezone to consider the date as if it were in the 
timezone of the user's machine (on a computer on UTC, the 01/01/2000 12:00 in UTC+3 would become 01/01/2000 12:00 UTC+0).
If a date had to be expressed in its original timezone, the date specification required an extra `x-date-timezone` vendor.
As of version 11, the default behavior is now to keep the timezone of the API response for date-time objects. They
would be revived as `Date` or kept as `string` depending on the value of the `stringifyDate` option.
The removal the timezone is now an 'opt-in' mechanism triggered by the `x-local-timezone` vendor.

> [!NOTE]
> This change only impacts `date-time` format. `Date` format will still be revived as `utils.Date` objects as the timezone 
> should always be ignored for date object (since there is no concept of time).

> [!NOTE]
> You can revert to the previous behavior thanks to the global property `useLegacyDateExtension`
<!-- generated deprecated - start -->
## Deprecated this version

The following items are deprecated and will be removed in next major version:


### From [@o3r/components](https://npmjs.com/package/@o3r/components):

- `CapitalizePipe` will deprecated with the following message:</br>*please use O3rCapitalizePipe, will be removed in v12.*
- `CapitalizePipeModule` will deprecated with the following message:</br>*please use O3rCapitalizePipe, will be removed in v12.*
- `DurationPipe` will deprecated with the following message:</br>*please use O3rDurationPipe, will be removed in v12.*
- `DurationPipeModule` will deprecated with the following message:</br>*please use O3rDurationPipe, will be removed in v12.*
- `KeepWhiteSpacePipe` will deprecated with the following message:</br>*please use O3rKeepWhiteSpacePipe, will be removed in v12.*
- `KeepWhiteSpacePipeModule` will deprecated with the following message:</br>*please use O3rKeepWhiteSpacePipe, will be removed in v12.*
- `ReplaceWithBoldPipe` will deprecated with the following message:</br>*please use O3rReplaceWithBoldPipe, will be removed in v12.*
- `ReplaceWithBoldPipeModule` will deprecated with the following message:</br>*please use O3rReplaceWithBoldPipe, will be removed in v12.*

### From [@o3r/configuration](https://npmjs.com/package/@o3r/configuration):

- `computeConfigurationName` will deprecated with the following message:</br>*use {@link computeItemIdentifier} from @o3r/core. Will be removed in v12*
- `ConfigObserver` will deprecated with the following message:</br>*please use O3rConfig, will be removed in v12*
- `configSignal` will deprecated with the following message:</br>*please do not provide `configurationService`, will be removed in v12*
- `loadConfiguration` will deprecated with the following message:</br>*please use `updateConfigurations` instead, will be removed in Otter v12.*

### From [@o3r/dev-tools](https://npmjs.com/package/@o3r/dev-tools):

- `checkoutBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `checkoutCrtRcBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `checkoutCurrentAlphaOrNextBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `commitChangeLogs` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `createNextAlphaBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `createNextMajorBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `createRcBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `createReleaseBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `deleteBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `deleteCrtRcBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `deleteCurrentAlphaOrNextBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `deletePreviousReleaseBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `GavcResponse` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `getCrtRcBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `getCurrentAlphaOrNextBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `getJestModuleNameMapper` will deprecated with the following message:</br>*Please use `pathsToModuleNameMapper` from `ts-jest`, will be removed in Otter v12.*
- `getJestProjects` will deprecated with the following message:</br>*Please use the one exposed in `@o3r/workspace`, will be removed in Otter v12.*
- `getPackageManager` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `getPackagesToInstallOrUpdate` will deprecated with the following message:</br>*You can use the one exposed in `@o3r/schematics`, will be removed in Otter v12.*
- `isPackageInstalled` will deprecated with the following message:</br>*You can use the one exposed in `@o3r/schematics`, will be removed in Otter v12.*
- `PackageVersion` will deprecated with the following message:</br>*You can use the one exposed in `@o3r/schematics`, will be removed in Otter v12.*
- `pad` will deprecated with the following message:</br>*You can use the one exposed in `@ama-sdk/core`, will be removed in Otter v12.*
- `PullRequestService` will deprecated with the following message:</br>*will be removed in v12. Please use `PullRequestService` from `@o3r/azure-tools`*
- `updatePeerDependencies` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `updateRenovateBaseBranch` will deprecated with the following message:</br>*will be removed in Otter v12.*

### From [@o3r/dynamic-content](https://npmjs.com/package/@o3r/dynamic-content):

- `DynamicContentPipe` will deprecated with the following message:</br>*please use O3rDynamicContentPipe, will be removed in v12. */*

### From [@o3r/localization](https://npmjs.com/package/@o3r/localization):

- `LocalizationTranslatePipe` will deprecated with the following message:</br>*please use O3rLocalizationTranslatePipe, will be removed in v12.*

### From [@o3r/rules-engine](https://npmjs.com/package/@o3r/rules-engine):

- `ActionOverrideBlock` will deprecated with the following message:</br>*will be removed in Otter v12.*
- `ActionUpdateAssetBlock` will deprecated with the following message:</br>*use the one exposer by `@o3r/dynamic-content` module. Will be removed in Otter v12.*
- `ActionUpdateConfigBlock` will deprecated with the following message:</br>*use the one exposer by `@o3r/configuration` module. Will be removed in Otter v12.*
- `ActionUpdateLocalisationBlock` will deprecated with the following message:</br>*use the one exposer by `@o3r/localization` module. Will be removed in Otter v12.*
- `ActionUpdatePlaceholderBlock` will deprecated with the following message:</br>*use the one exposer by `@o3r/components` module. Will be removed in Otter v12.*
- `FallbackToPipe` will deprecated with the following message:</br>*please use O3rFallbackToPipe, will be removed in v12.*
- `inputFacts` will deprecated with the following message:</br>*will be removed in v12. Facts that are needed for the rule execution (sent by the CMS) */*
- `linkedComponent` will deprecated with the following message:</br>*It will be removed in v12, use {@link linkedComponents} instead*
- `linkedComponent` will deprecated with the following message:</br>*It will be removed in v12, use {@link linkedComponents} instead*
- `linkRulesetToComponent` will deprecated with the following message:</br>*; It will be removed in v12 with the selector using it*
- `selectRuleSetLinkComponents` will deprecated with the following message:</br>*use {@link selectComponentsLinkedToRuleset} instead. It will be removed in v12*

### From [@o3r/schematics](https://npmjs.com/package/@o3r/schematics):

- `isStandaloneRepository` will deprecated with the following message:</br>*no longer in use. Will be removed in V12*
- `setupSchematicsDefaultParams` will deprecated with the following message:</br>*will be removed in v12. Use {@link setupSchematicsParamsForProject}*

### From [@o3r/styling](https://npmjs.com/package/@o3r/styling):

- `deviceBreakpoints` will deprecated with the following message:</br>*use Breakpoints exported by @angular/cdk instead, will be removed in v12*
- `StyleLazyLoader` will deprecated with the following message:</br>*use StyleLazyLoader exported by @o3r/dynamic-content instead, will be removed in v12*
- `StyleLazyLoaderModule` will deprecated with the following message:</br>*use StyleLazyLoaderModule exported by @o3r/dynamic-content instead, will be removed in v12*
- `StyleURL` will deprecated with the following message:</br>*use StyleURL exported by @o3r/dynamic-content instead, will be removed in v12*
<!-- generated deprecated - end -->