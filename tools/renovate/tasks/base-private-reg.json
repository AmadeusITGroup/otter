{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Trigger post install scripts when updating package manager with private registry support. arg0: npm config file path (e.g. '.npmrc-renovate'). Only supported for npm and Yarn 1 via NPM_CONFIG_USERCONFIG; for Yarn 2+, configure hostRules instead (see https://docs.renovatebot.com/getting-started/private-packages/#yarn-2).",
  "packageRules": [
    {
      "matchDepTypes": [
        "packageManager"
      ],
      "matchPackageNames": [
        "npm",
        "yarn"
      ],
      "postUpgradeTasks": {
        "commands": [
          "NPM_CONFIG_USERCONFIG='{{arg0}}' {{{depName}}} install"
        ],
        "executionMode": "branch",
        "fileFilters": [
          "packages/**/package.json",
          "packages/**/package.json.template",
          "package.json"
        ]
      },
      "groupName": "Package manager",
      "groupSlug": "package-manager"
    }
  ]
}
