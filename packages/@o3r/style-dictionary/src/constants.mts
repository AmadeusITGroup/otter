/** Token Key used to propagate the Extension for Token Extension files */
export const OTTER_EXTENSIONS_NODE_NAME = '__extensions__';

/** Prefix of the expose Design Token items */
export const OTTER_NAME_PREFIX = 'o3r';
