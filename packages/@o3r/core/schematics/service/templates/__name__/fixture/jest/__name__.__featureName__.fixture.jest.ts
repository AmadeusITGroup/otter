import {<%= serviceName %>} from '<%= packageName %>/<%= name %>';

/**
 * Jest fixture for <%= name %> <%= featureName %> service
 */
export class <%= serviceName %>Fixture implements Readonly<<%= serviceName %>> {
}
