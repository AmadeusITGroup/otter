/**
 * Block component
 * Components which are used as blocks should implement this to be identified by CMS extractor
 *
 * @deprecated use `@O3rComponent({componentType: 'Block'})`, will be removed in v10
 */
export interface Block {}
