/**
 * Page component
 * Components which are used as application pages should implement this to be identified by CMS extractor
 *
 * @deprecated use `@O3rComponent({componentType: 'Page'})`, will be removed in v10
 */
export interface Page {}
