/**
 * Exposed component
 * To be implemented by components meant to be exposed through the CMS but that are not blocks or pages
 * It's useful both for config and components linkable to ruleset
 *
 * @deprecated use `@O3rComponent({componentType: 'ExposedComponent'})`, will be removed in v10
 */
export interface ExposedComponent {}
