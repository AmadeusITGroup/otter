{
  "$schema": "http://json-schema.org/schema",
  "$id": "ngAddSchematicsSchema",
  "title": "Add Otter rules-engine ",
  "description": "ngAdd Otter rules-engine ",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "Project name",
      "$default": {
        "$source": "projectName"
      }
    },
    "enableMetadataExtract": {
      "type": "boolean",
      "default": false,
      "description": "Activate metadata extraction"
    }
  },
  "additionalProperties": true,
  "required": [
  ]
}
