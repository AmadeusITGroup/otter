import type {
  MigrationMetadataCheckBuilderOptions,
} from '@o3r/extractors';

/** Migration metadata check builder schema */
export interface RuleFactMigrationMetadataCheckBuilderSchema extends MigrationMetadataCheckBuilderOptions {
}
