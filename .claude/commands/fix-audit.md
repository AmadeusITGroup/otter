# Fix Audit Vulnerabilities

Fix the critical audit vulnerabilities blocking a PR. Usage: `/fix-audit <pr_number>`

Follow the instructions in [`.agents/fix-audit.md`](../../.agents/fix-audit.md), replacing every occurrence of `PR_NUMBER` with `$ARGUMENTS`.
