---
agent: agent
description: Fix critical audit vulnerabilities blocking a PR. Provide the PR number as input.
---

Follow the instructions in [`.agents/fix-audit.md`](../../.agents/fix-audit.md), replacing every occurrence of `PR_NUMBER` with `${input:pr_number}`.
